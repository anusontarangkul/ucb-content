// Determine what operating user person is using

// var Name = "Not known";
//         if (navigator.appVersion.indexOf("Win") != -1) Name =
//           "Windows OS";
//         if (navigator.appVersion.indexOf("Mac") != -1) Name =
//           "MacOS";
//         if (navigator.appVersion.indexOf("X11") != -1) Name =
//           "UNIX OS";
//         if (navigator.appVersion.indexOf("Linux") != -1) Name =
//           "Linux OS";

// Welcome user based on operating system

alert("Welcome, Mac User!");

// Determine the coordinates of person

// redirect person to either eastern or western html based on location
